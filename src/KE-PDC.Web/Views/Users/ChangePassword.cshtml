@model ChangePasswordViewModel

@{
    var InvalidState = Microsoft.AspNetCore.Mvc.ModelBinding.ModelValidationState.Invalid;
    ViewData["Title"] = "Change Password";
}
<style>
    body {
        margin: 0;
    }
</style>
<iframe src="https://kesso.th.kerryexpress.com/sso/ChangePassword/" style="width:100%; border:none" height="500"></iframe>

@*<div class="row">
    <div class="col-md-6 col-md-offset-3">
        <div class="card">
            <div class="card-header card-header-alt">
                <h2 class="card-title">@Localizer["Change Password"]</h2>
            </div>
            <div class="col-md-12">
                <iframe src="http://10.112.86.146/sso/ResetPassword"></iframe>
            </div>
            
            <div class="card-body card-padding">
            @if (ViewData.ModelState.GetFieldValidationState("failed") == InvalidState)
            {
                <div class="alert alert-danger alert-dismissible" role="alert">
                    <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                    <strong>Sorry!</strong> @ViewData.ModelState["failed"].Errors.FirstOrDefault().ErrorMessage
                </div>
            }

            @if (ViewData.ModelState.GetFieldValidationState("succeed") == InvalidState)
            {
                <div class="alert alert-success alert-dismissible" role="alert">
                    <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                    @ViewData.ModelState["succeed"].Errors.FirstOrDefault().ErrorMessage
                </div>
            }

            <form asp-action="ChangePassword" asp-controller="Users" asp-antiforgery="true" method="post">
                <div class="form-group@(ViewData.ModelState.GetFieldValidationState("OldPassword") == InvalidState ? " has-error  has-feedback":"" )">
                    <label for="InputOldPassword1">Old Password</label>
                    <input asp-for="OldPassword" class="form-control" id="InputOldPassword1">
                    @if (ViewData.ModelState.GetFieldValidationState("OldPassword") == InvalidState)
                    {
                        <span class="ion-close form-control-feedback" aria-hidden="true"></span>
                    <span asp-validation-for="OldPassword" class="help-block"></span>
                    }
                </div>
                <hr />
                <div class="form-group@(ViewData.ModelState.GetFieldValidationState("Password") == InvalidState ? " has-error  has-feedback":"" )">
                    <label for="InputNewPassword1">New Password</label>
                    <input asp-for="Password" class="form-control" id="InputNewPassword1">
                    @if (ViewData.ModelState.GetFieldValidationState("Password") == InvalidState)
                    {
                        <span class="ion-close form-control-feedback" aria-hidden="true"></span>
                        <span asp-validation-for="Password" class="help-block"></span>
                    }
                </div>
                <div class="form-group@(ViewData.ModelState.GetFieldValidationState("ConfirmationPassword") == InvalidState ? " has-error  has-feedback":"" )">
                    <label for="InputNewPassword1">New Password</label>
                    <input asp-for="ConfirmationPassword" class="form-control" id="InputNewPassword1">
                    @if (ViewData.ModelState.GetFieldValidationState("ConfirmationPassword") == InvalidState)
                    {
                        <span class="ion-close form-control-feedback" aria-hidden="true"></span>
                        <span asp-validation-for="ConfirmationPassword" class="help-block"></span>
                    }
                </div>
                <div class="text-right">
                    <button type="submit" class="btn btn-outline-primary">Save Change</button>
                </div>
            </form>
        </div>
        </div>
    </div>
</div>*@